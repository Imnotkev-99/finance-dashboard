{
  "name": "apex-finance-dashboard",
  "version": "1.0.0",
  "description": "Dashboard de finanzas personales con Supabase y Chart.js (vanilla JS, sin build).",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "verify": "npm run lint && npm test && npm run test:e2e",
    "lint": "eslint js",
    "format": "prettier --write \"**/*.{js,css,html,json,md}\""
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "eslint": "^9.13.0",
    "prettier": "^3.3.3",
    "vitest": "^4.1.9"
  }
}
